$('#comment-form-ajax').html('<%= j render 'comments/form', :@comment => Comment.new, :review => @review %>');
$('#comment-list-ajax').prepend('<%= j render @new_comment %>');
$('#comment-count').html('Comment (<%= @review.comments.count %>)')