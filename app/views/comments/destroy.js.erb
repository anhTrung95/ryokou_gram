$('#comment-card-<%=@comment.id%>').remove();
$('#comment-count-<%= @review.id %>').html('Comment (<%= @review.comments.count %>)');
$('#si-comments-<%= @review.id %>').html('<%= @review.comments.count %>')